# 바이브마스터 버전2 — 웹서비스 개발

**무엇** — 직장인·비개발자 한 사람이 **웹서비스 하나를 기획해서 만들고 내놓는 여정**을 Stage 단위로 쓴 학습웹.
**이 폴더** = 옵시디언 vault = GitHub 새 저장소. 셋이 하나다.
**재료는 고치지 않는다.** 원본은 읽기만 하고, 카드가 아니라 절로 다시 쓴다. (재료 폴더는 공개 저장소에 올리지 않는다 — 남의 저작물이 섞여 있다.)

## 첫 번째 규칙 — THE CHAIN

앞 Stage의 **산출물이 뒤 Stage의 재료**가 된다. 이게 끊기면 사전이 되고, 사전이 되면 실패한다(올라비즈).
Stage를 쓸 때마다 묻는다 — **이 장에서 나온 파일이 다음 장 어디에서 쓰이나?**

## 절 하나 = md 파일 하나

```yaml
---
스테이지: 0            # 0 1 2 3 4 4.5 5 6 7 8 9 10 / 업무자동화 / 부록
순서: 1
제목: 도구 지도 — 몇 개를 쓰나
관통: 도구는 둘이면 된다. 메인 하나, 백업 하나.   # 절을 한 줄로 기억하게 하는 말
소요: 20분
준비물: [Claude 계정]                            # 없으면 []
갱신: 2026-09-09
---
```

본문은 `## ` 칸. **이 순서를 지킨다:**

| 칸 | 언제 | 무엇 |
|---|---|---|
| `## 왜 이걸 하나` | 항상 | 이 절을 건너뛰면 뒤에서 무엇이 막히는지 |
| 자유 제목 여럿 | 항상 | 본문 |
| `## 📘 말 풀이` | 새 용어가 나오면 | 용어 하나당 두 줄. **한 절에 셋을 넘으면 절을 쪼갠다** |
| `## ⚠️ 자주 걸리는 함정` | 걸릴 곳이 있으면 | 초보자가 실제로 걸리는 자리 |
| `## ⚡ 이렇게 하세요` | 항상 | 그대로 따라 하면 돌아가는 것. 프롬프트는 코드블록 |
| `## ✅ 됐는지 확인` | 항상 | **화면에 무엇이 보이면 성공인가.** 예/아니오로 |
| `## 🔎 더 깊이` | 깊게 갈 길이 있으면 | 공식 문서 주소 · 무엇을 검색할지 |

Stage 폴더마다 `_스테이지.md` 하나 — 학습 목적 · 시작 전 준비물 · 손에 남는 것 · 자가 체크 · 다음으로.
파일명 `순서-짧은이름.md`. 폴더는 `원고/00-환경/` 처럼.

## 쓰는 규칙

- **읽는 사람은 직장인·비개발자다.** 12살에게 설명하듯. 기술용어·코드·에러는 영어 원문 유지.
- **자가 체크는 예/아니오로 답할 수 있게.** "이해했나"는 체크가 아니다. "화면에 ○○이 보이나"가 체크다.
- **설치가 필요한 것은 본문에 이름을 쓴다.** 요금·버전만 부록으로. (이름을 다 빼면 아무것도 못 깐다)
- **본문에 넣지 않는 것** — 모델명 · 요금 · 버전 번호 · 개인정보(이메일·전화·실명) · 또박이 · 폐업 브랜드를 현재형으로.
- 벤더 홍보 수치 인용 금지. 1차·2차 출처만, 등급과 날짜를 붙인다.
- 확인 못 한 것은 **[확인 필요]**로 남긴다. 추측을 사실처럼 쓰지 않는다.
- 링크는 **상대경로만**. v1 부록이 절대경로라 404가 됐다.
- **원본을 붙이지 않는다.** 다시 쓴다. 원본은 「🔎 더 깊이」에서 가리킨다.
- 재료 폴더는 읽기만. 편집 금지.
- 특정 회사의 사실을 여기 쓰지 않는다. 이 교재는 누가 읽어도 통해야 한다.

## 발행

```
python3 발행/build.py        # → 발행/바이브마스터v2.html
```
**같은 파일을 같은 아티팩트에 재발행한다.** 새로 만들면 링크가 바뀌고 진도가 사라진다.
검색은 **페이지 안 JS로.** 외부 빌드(Pagefind)에 맡기지 않는다 — v1이 그래서 죽었다.
Stage 하나 끝나면 다시 굽고 한 줄로 알린다. 쌓아서 한 번에 보여주지 않는다.

## 하지 않는 것

- v1 원본을 고치지 않는다. 재료는 재료로만 쓴다
- 올라비즈를 지우거나 되살리지 않는다. 3권 30장만 재료로 가져온다
- 카드 사전 구조로 돌아가지 않는다. Stage 여정을 지킨다
- 다 쓰고 한 번에 보여주지 않는다. Stage 하나씩

## 호칭

직함이 아니라 하는 일로. "비서실장" 같은 표현 쓰지 않는다.
